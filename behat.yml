default:
    suites:
        default:
            contexts:
                - FeatureContext
                - SecurityContext
                - NavigationContext
                - UserContext
                - TaskContext
